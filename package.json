{
  "name": "messenger",
  "homepage": "https://github.com/userlike/messenger",
  "repository": {
    "type": "git",
    "url": "https://github.com/userlike/messenger.git"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -At --no-private run build",
    "lint": "yarn workspaces foreach -At --no-private run lint",
    "test": "yarn workspaces foreach -At --no-private run test",
    "postinstall": "lefthook install"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.1",
    "@commitlint/config-conventional": "^21.0.1",
    "@eslint/js": "^10.0.1",
    "eslint": "^10.4.0",
    "jest": "^30.4.2",
    "lefthook": "^2.1.8",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.59.4",
    "webpack": "^5.107.1",
    "webpack-cli": "^7.0.2"
  },
  "packageManager": "yarn@4.1.1"
}
